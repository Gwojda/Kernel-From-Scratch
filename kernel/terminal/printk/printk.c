#include "printk.h"

static int	get_number_element(const char *fmt)
{
	size_t i = 0;

	while (fmt[i])
	{

		++i;
	}
}

int		printk(const char *fmt, ...)
{
	va_list	ap;

	va_start(ap, count);
	va_end(ap);
}
